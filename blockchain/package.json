{
  "name": "healthcare-blockchain",
  "version": "1.0.0",
  "description": "Blockchain Healthcare System",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "add-doctor": "hardhat run scripts/addDoctor.ts --network localhost",
    "add-doctor:sepolia": "hardhat run scripts/addDoctor.ts --network sepolia",
    "seed": "hardhat run scripts/seed.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.16.0"
  }
}
